// Type declarations for subtitle-utils.cjs
// Used by TypeScript frontend when importing via bundler CJS interop.

export function hexToAssColor(hexStr: string, alphaHex?: string): string;
export function assTime(sec: number): string;
export function cleanPunct(word: string): string;
